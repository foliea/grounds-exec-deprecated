<%= content_tag(:div, @ground.content, id: :grounds_editor, data: @ground.editor) %>
<%= form_for(@ground, as: :ground, url: grounds_run_path, remote: true) do |f| %>
  <%= f.hidden_field :code %>
  <%= f.submit %>
<% end %>
